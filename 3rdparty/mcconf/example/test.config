[config]

moduledirs = [ "modules"] # search pathes relative to position of the config file
destdir = 'exconf'
provides = [ "x86"] # pseudo modules and so on that are assumed as available
requires = [ 'a.h' ]
modules = [ 'modb', 'moda', 'modc' ] # all modules that define this configuration
